5000
# Psych Ward OS — настройки сервиса balancer (см. ROADMAP.md, Фаза 9.B).
# Число ВЫШЕ (первое, что стоит в файле) — интервал между авто-запусками
# SYS_BALANCE, в мс. Парсер (src/service/balancer.cpp) читает только
# ведущие цифры в самом начале файла, всё остальное — не разбирает и
# игнорирует, поэтому комментарии должны идти ПОСЛЕ числа, не до. Если
# файла нет или он не начинается с цифры — используется встроенное
# значение по умолчанию (тоже 5000 мс).
